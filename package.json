{
  "name": "controle-brindes",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend (React + Vite) do sistema de Controle de Brindes — dados no Supabase, deploy no Vercel",
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "dev":         "npm run dev   --prefix frontend",
    "build":       "npm run build --prefix frontend",
    "preview":     "npm run preview --prefix frontend"
  }
}
